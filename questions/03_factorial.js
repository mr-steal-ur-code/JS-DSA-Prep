/*3. Factorial Calculation
Implement a function to compute the factorial of a non-negative integer using recursion. The factorial of
a number n is the product of all positive integers less than or equal to n, denoted as n!.

Input: 5
Output: 120 (since 5! = 5 × 4 × 3 × 2 × 1 = 120)
*/

const factorial = (num) => {

}
console.log(factorial(5));
console.log(factorial(5));
console.log(factorial(10));
console.log(factorial(150));
