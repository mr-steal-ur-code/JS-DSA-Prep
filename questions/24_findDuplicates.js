/*24. Find Duplicates
Given an array of integers, write a function to find and return all duplicate elements. The function should
return the duplicates in any order and should not include any element more than once.

Input: array = [1, 2, 3, 2, 3, 4, 5]
Output: [2, 3]
*/

const findDupes = (arr) => {

};
console.log(findDupes([1, 2, 3, 2, 3, 4, 5]));
