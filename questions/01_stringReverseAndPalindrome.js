/*1. Reverse a String
Write a function that takes a string as input and returns a new string with the characters in reverse order.
Do not use any built-in reverse functions or libraries.

Input: "hello"
Output: "olleh"
*/

const revStr = (str) => {

};
console.log(revStr("hello"));

/*1.5. Palindrome Checker
Create a function to check if a given string is a palindrome. 
A palindrome is a word or phrase that reads the same backward as forward, ignoring spaces, punctuation, and capitalization.

Input: "A man a plan a canal Panama"
Output: true (ignoring spaces and capitalization) 
Input: "hello" 
Output: false 
*/

const isPalindrome = (str) => {

};
console.log(isPalindrome("A man a plan a canal Panama"));
